[pytest]
testpaths = tests
pythonpath = .
addopts =
    -v
    --html=reports/report.html
    --self-contained-html
markers =
    smoke: smoke tests
    regression: regression tests
    negative: negative / error-path tests
    api: API-level tests using Playwright's APIRequestContext
